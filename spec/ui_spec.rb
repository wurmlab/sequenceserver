describe 'ui' do
  # NOTE: unless #75 is resolved, make sure to test this functionality only if
  # HTML5 autofocus attribute is supported
  it 'should automatically focus sequence input box'

  # NOTE: only if the browser supports it
  it 'should ensure spell checking is disabled for sequence input box'

  it 'should sort the list of databases in alphabetical order'

  it 'should pre-select if only one databae present'
end
